package com.Interface.bridge;

public interface Ball {
    Ball ballDetails(Ball ball);
    int runs(Ball ball);
    String name(Ball ball);
    Double percent(Ball ball);
    Boolean isball(Ball ball);
}
